# python-ui-auto-test dependencies
# Tested on Python 3.11
# Install: pip install -r requirements.txt

# Web automation (项目代码用的是 Selenium 3 风格的 find_element_by_xpath,
# 在 Selenium 4.x 上需要迁移 — 详见 README "已知问题" 章节)
selenium>=3.141.0,<4.0.0

# HTML test report
BeautifulReport>=0.1.3

# Parameterized unittest
paramunittest>=0.2

# Multithread decorator (for run_all_mutithread.py)
tomorrow>=0.2.0

# MySQL client
pymysql>=1.0.2

# Redis client
redis>=3.5.0,<5.0.0

# ── pytest 兼容层 (2026-06 新增) ─────────────────────────────────────
# pytest 本身：让项目能用现代测试框架跑，不改 unittest 风格 case
pytest>=7.0.0

# pytest-html：生成独立 HTML 报告（不依赖 BeautifulReport）
pytest-html>=3.2.0

# pytest-xdist：并行跑测试（替代 tomorrow 的多线程方案）
pytest-xdist>=3.0.0

# pytest-timeout：单测试超时保护（selenium 卡死时自动失败）
pytest-timeout>=2.1.0

# ── v2 Playwright 兼容层 (2026-06 新增) ──────────────────────────────
# 新一代浏览器自动化: 跨浏览器、自动等待、不用装 WebDriver
# playwright: 核心 SDK
# pytest-playwright: pytest 集成,提供 page/browser/context fixtures
# CI 跑 UI 还需要: playwright install --with-deps chromium
playwright>=1.40.0
pytest-playwright>=0.5.0
